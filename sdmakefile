
.ifdef bootstrap
SDMAKE = bin/sdmake
.else
SDMAKE = sdmake
.endif

Types = OS_v34 OS_v36 OS_v37 OS_v39 OS_v47

.ifgt $(HOST_OSVER) 46
NATIVE = OS_v47
.else
.ifgt $(HOST_OSVER) 38
NATIVE = OS_v39
.else
.ifgt $(HOST_OSVER) 35
NATIVE = OS_v36
.else
.ifgt $(HOST_OSVER) 36
NATIVE = OS_v37
.else
NATIVE = OS_v34
.endif
.endif
.endif
.endif

.leftislabel

native: $(NATIVE)

.ifdef INSTALL
$(NATIVE):
    $(SDMAKE) -f SDMakefile.main -DTYPE=%(left) -DOSVERMIN=%(left:OS_v*:*) -DOSVERMAX=%(left:OS_v*:*) bootstrap
.else
$(NATIVE):
    $(SDMAKE) -f SDMakefile.main -DTYPE=%(left) -DOSVERMIN=%(left:OS_v*:*) -DOSVERMAX=%(left:OS_v*:*)
.endif


Generic:
    $(SDMAKE) -f SDMakefile.main -DTYPE=%(left)

$(Types)::
    $(SDMAKE) -f SDMakefile.main -DTYPE=%(left) -DOSVERMIN=%(left:OS_v*:*) -DOSVERMAX=%(left:OS_v*:*)

all: $(Types)



